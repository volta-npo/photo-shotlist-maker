{
  "name": "@volta-oss/photo-shotlist-maker",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "Generate practical photo shotlists for restaurants, services, nonprofits, and founder-led teams.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/photo-shotlist-maker:local .",
    "docker:run": "docker compose up --build"
  },
  "keywords": [
    "digital-equity",
    "local-first",
    "marketing-content",
    "oss",
    "v3-production",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/photo-shotlist-maker.git"
  },
  "homepage": "https://volta-npo.github.io/photo-shotlist-maker/",
  "bugs": {
    "url": "https://github.com/volta-npo/photo-shotlist-maker/issues"
  },
  "author": "Volta OSS Division <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.9.1"
  }
}
